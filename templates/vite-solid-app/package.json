{
  "name": "vite-solid-app",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite --clearScreen false",
    "build": "vite build",
    "deploy": "vercel deploy dist --team=turborepo --confirm",
    "lint": "TIMING=1 eslint src/**/*.tsx --fix && tsc --noEmit",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
  },
  "dependencies": {
    "@vinxi/vite-solid-lib": "workspace:^0.0.0",
    "solid-js": "^1.3.0-beta.10"
  },
  "devDependencies": {
    "@vinxi/scripts": "*",
    "@vinxi/tsconfig": "*",
    "typescript": "^4.5.3",
    "vite": "^2.6.14",
    "vite-plugin-solid": "^2.1.4"
  }
}
